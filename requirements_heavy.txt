# Heavy dependencies - CACHED LAYER (rarely changes)
# These are ~3GB+ combined and should NOT be reinstalled on code changes

# PyTorch CPU-only (much smaller than CUDA version: ~200MB vs 2GB)
# If you have NVIDIA GPU, change to: --extra-index-url https://download.pytorch.org/whl/cu121
torch>=2.0.0
torchvision>=0.15.0

# RAG Memory (700MB+)
chromadb>=0.5.0
sentence-transformers>=2.2.0

# Boosting Libraries (150MB+)
xgboost>=2.0.0
lightgbm>=4.0.0
scikit-learn>=1.3.0
