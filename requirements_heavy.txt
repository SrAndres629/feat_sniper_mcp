# Heavy dependencies - CACHED LAYER (rarely changes)
# These are ~3GB+ combined and should NOT be reinstalled on code changes

# PyTorch with CUDA 12.4 support (Optimized for RTX 3060)
# Install manually with: pip install --index-url https://download.pytorch.org/whl/cu124 torch
# torch>=2.5.0  # Commented out - install via CUDA index above

# RAG Memory (700MB+)
chromadb>=0.5.0
sentence-transformers>=2.2.0

# Boosting Libraries (150MB+)
xgboost>=2.0.0
lightgbm>=4.0.0
scikit-learn>=1.3.0
